# Examples

- [`demo/`](./demo) — a reproducible, runnable version of the quickstart: two
  small git repositories and one bilingual notes file, with the exact
  `DECISIONS.md` and terminal output that `amanos init` + `amanos scan`
  produce from them. CI runs it on every push and fails if a real run
  diverges from the committed golden files.
